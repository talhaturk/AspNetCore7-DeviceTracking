@model IEnumerable<UserDevices>

<div class="col-md-4">

</div>

<div class="col-md-8">
    <div class="row">
        @foreach (UserDevices userDevices in @Model)
        {
            <partial name="_DeviceCard" model=@userDevices/>    
        }
    </div>
</div>